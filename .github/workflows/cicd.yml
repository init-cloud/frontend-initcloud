name: FRONTEND-DEV CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@master

      - name: Install Dependencies
        uses: npm install

      - name: React Build
        uses: npm run build
            